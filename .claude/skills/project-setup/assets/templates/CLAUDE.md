# {{PROJECT_NAME}}

<!-- Project memory. Always in context, so keep it under ~100 lines and mostly pointers.
     Delete what stops being true — that's the maintenance work. -->

{{ONE_LINER}}

## Goals

<!-- Two or three lines: what this is, who it's for, what is explicitly out of scope right now. -->

## Architecture overview

<!-- The shape in a few lines — enough to guess where a file lives. Detail lives in docs/ARCHITECTURE.md. -->

See `docs/ARCHITECTURE.md`.

## Style guide

<!-- Code conventions, naming, file layout. If there's a UI, the visual rules that keep it coherent. -->

## Constraints and policies

<!-- The expensive-to-violate rules. Examples:
     - Never commit .env or any real API key
     - A single <operation> must cost under $X
     - <data> never leaves the local machine
     - Don't add dependencies without asking -->

## Repository etiquette

- Branch naming: 
- Commits: use `/update-docs-and-commit` so docs stay in sync with code
- PRs: 

## Commands

```bash
# dev
# test
# lint
# build
```

## Documentation

- `docs/project_spec.md` — requirements, milestones, engineering design (source of truth)
- `docs/ARCHITECTURE.md` — system overview and component architecture
- `docs/PROJECT_STATUS.md` — milestones, what's done, what's next
- `docs/CHANGELOG.md` — history of changes
- `docs/reference/` — per-feature reference docs
